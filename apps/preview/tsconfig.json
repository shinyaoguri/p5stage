{
  "extends": "../../tsconfig.base.json",
  "compilerOptions": {
    "lib": ["ESNext"]
  },
  // バインディングと変数の型 (グローバルの `Env`) と Workers ランタイム型は
  // wrangler.jsonc から `wrangler types` が生成する。
  "include": ["src", "test", "worker-configuration.d.ts"]
}
